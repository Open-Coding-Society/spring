# spring

This repo is one of three siblings (`spring`, `flask`, `pages`) that make up
one full-stack app, versioned separately under a shared `prodsys` parent
folder. Before doing anything else, read `../.clauderc` (one directory up)
-- it has the project structure, ongoing work, deploy requirements, and
known gaps that aren't discoverable from this repo's code alone.
